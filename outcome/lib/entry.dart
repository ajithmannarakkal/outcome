import 'package:flutter/material.dart';

class EntryIn extends StatelessWidget {
  const EntryIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add Your Income"),),
    );
  }
}
